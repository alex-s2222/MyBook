# берем определенную версию debian 
# FROM строго обязательная команда 
FROM debian 

# определяем команды выполняемые в командной оболочке 
RUN apt-get update 
RUN apt-get install -y cowsay fortune

# копирует файл из файловой системы хоста в файловую систему образа 
# первый аргумент определяет файл хоста а второй целевой путь
COPY entrypoint.sh /


# Инструкция позволяет определить выполняемый файл, который будет 
# вызываться для обработки любых аргументов переданных в комвнду 
#old # ENTRYPOINT ["/usr/games/cowsay"]
ENTRYPOINT ["/entrypoint.sh"]
